// EverShop page middleware entry for `/admin/dark-mode`.
// In EverShop, per-page middleware files are typically named `index.ts`.

export default function adminDarkModeIndex(request: any, response: any) {
  return { request, response };
}

